import { RequestInterceptorInterceptor } from './request-interceptor.interceptor';

describe('RequestInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new RequestInterceptorInterceptor()).toBeDefined();
  });
});
